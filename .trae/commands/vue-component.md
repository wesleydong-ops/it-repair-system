---
name: 组件模板
description: 生成标准的 Vue 组件模板代码
---

请生成标准的 Vue 3 组件模板：

1. **组件结构**：
   - `<script setup lang="ts">` 组合式 API
   - `<template>` 模板
   - `<style scoped>` 样式

2. **标准内容**：
   - Props 定义和类型
   - Emits 定义
   - 响应式数据（ref/reactive）
   - 计算属性（computed）
   - 生命周期钩子（onMounted/onUnmounted）
   - 方法定义

3. **最佳实践**：
   - 使用 TypeScript 类型
   - 合理的代码分组
   - 清晰的命名

请根据组件用途生成合适的模板代码。